{
  "name": "watch-your-breath",
  "private": true,
  "version": "0.1.0",
  "description": "A gentle reminder to notice the breath that’s already happening.",
  "workspaces": [
    "illustrations",
    "extension",
    "web"
  ],
  "scripts": {
    "crop": "npm run crop -w illustrations",
    "dev:web": "npm run dev -w web",
    "build:web": "npm run build -w web",
    "build:extension": "npm run build -w extension",
    "package:extension": "npm run package:all -w extension && npm run sync-downloads",
    "build": "npm run build:extension && npm run build:web",
    "sync-downloads": "node scripts/sync-downloads.mjs"
  }
}
