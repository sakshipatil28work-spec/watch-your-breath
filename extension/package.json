{
  "name": "@wyb/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Watch Your Breath Chrome extension (Manifest V3).",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "watch": "node scripts/build.mjs --watch",
    "package": "node scripts/build.mjs && node scripts/package.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "fflate": "^0.8.3",
    "typescript": "^5"
  },
  "dependencies": {
    "@wyb/illustrations": "*"
  }
}
