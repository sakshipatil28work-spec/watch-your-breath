{
  "name": "@wyb/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Watch Your Breath browser extension (Manifest V3): Chrome, Edge, Brave, Opera, Arc, Vivaldi and Firefox.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:firefox": "node scripts/build.mjs --target firefox",
    "build:all": "node scripts/build.mjs && node scripts/build.mjs --target firefox",
    "watch": "node scripts/build.mjs --watch",
    "package": "node scripts/build.mjs && node scripts/package.mjs",
    "package:firefox": "node scripts/build.mjs --target firefox && node scripts/package.mjs --target firefox",
    "package:all": "npm run package && npm run package:firefox",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.25.0",
    "fflate": "^0.8.3",
    "typescript": "^5"
  },
  "dependencies": {
    "@wyb/illustrations": "*"
  }
}
